# Client-side configuration. Only values safe for the browser may use the VITE_ prefix.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side secrets. Keep these out of VITE_ variables and never expose them to the browser.
OPENAI_API_KEY=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
